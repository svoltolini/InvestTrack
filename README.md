# InvestTrack

Clean slate — ready for a new project.

The previous project (a SwiftUI dividend-tracker app with a Saxo Bank
OpenAPI connection) was removed but is fully preserved in git history.
To browse or restore it:

```sh
git log --oneline          # find the last app commit (merge of PR #12)
git checkout 408a5b3 -- .  # restore all of its files into the working tree
```
